{
  "name": "heroku-deploy",
  "description": "Build scripts for Heroku",
  "engines": {
    "node": "18.17.0"
  },
  "scripts": {
  "clean": "rm -rf public",
  "build": "echo 'Skipping React build during Heroku deploy'",
  "deploy": "echo 'React already built, skipping deploy'",
  "heroku-postbuild": "echo 'No postbuild needed — React is prebuilt'"
},
  "dependencies": {
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "react-router-dom": "^6.19.0",
    "@loadable/component": "^5.15.3"
  }
}