{
  "name": "heroku-deploy",
  "description": "Build scripts for Heroku",
  "engines": {
    "node": "18.17.0"
  },
  "scripts": {
    "build": "echo '✅ Skipping React build — handled manually.'",
    "heroku-postbuild": "./bin/cleanup-before-slug.sh"
  },
  "dependencies": {
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "react-router-dom": "^6.19.0",
    "@loadable/component": "^5.15.3"
  }
}