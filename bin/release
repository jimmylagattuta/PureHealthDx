#!/bin/bash

echo "🔥 bin/release: removing large files before slug creation"

rm -f bin/node
rm -rf vendor/bundle
rm -rf vendor/ruby-3.1.2

cat <<EOF
---
default_process_types:
  web: bundle exec rails s
EOF
