<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <style>
    body { font-family: Arial, sans-serif; padding: 20px; font-size: 14px; }
    h1 { color: #333; }
    .sig-box { border: 1px solid #000; padding: 8px; margin-bottom: 24px; }
    .sig-label { font-weight: bold; margin-bottom: 4px; }
    .sig-img { max-width: 300px; height: auto; border: 1px solid #000; }
  </style>
</head>
<body>

<h1>Appointment Summary (Test)</h1>

<p><strong>Patient Name:</strong> <%= @patient_name %></p>


<% if @date.present? %>
  <p><strong>Date:</strong> <%= @date.to_date.strftime("%B %d, %Y") %></p>
<% else %>
  <p><strong>Date:</strong> (not provided)</p>
<% end %>



<p><strong>Notes:</strong></p>
<pre><%= @notes %></pre>

<h3>Signature</h3>

<% if @signature_inline.present? %>
  <div class="sig-box">
    <div class="sig-label">Signed by patient:</div>
    <img class="sig-img" src="<%= @signature_inline %>" alt="Patient Signature" />
  </div>
<% else %>
  <div class="sig-box">
    <div class="sig-label">No signature received.</div>
    <p>(No image available)</p>
  </div>
<% end %>


</body>
</html>
