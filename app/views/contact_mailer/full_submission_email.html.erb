<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Pure Health Submission</title>
    <style>
      body {
        font-family: 'Segoe UI', sans-serif;
        background-color: #f9f9f9;
        margin: 0;
        padding: 20px;
        color: #333;
      }

      .email-container {
        max-width: 700px;
        margin: auto;
        background: #fff;
        padding: 25px 30px;
        border-radius: 8px;
        box-shadow: 0 3px 10px rgba(0,0,0,0.05);
      }

      h2 {
        color: #009688;
        margin-bottom: 0.5em;
      }

      h3 {
        color: #444;
        border-bottom: 1px solid #eee;
        padding-bottom: 4px;
        margin-top: 2em;
        margin-bottom: 0.8em;
      }

      ul {
        list-style: none;
        padding-left: 0;
      }

      li {
        margin-bottom: 6px;
      }

      strong {
        display: inline-block;
        width: 210px;
        font-weight: 600;
        color: #333;
      }

      .footer {
        margin-top: 2em;
        font-size: 0.85em;
        color: #999;
        text-align: center;
      }

      .highlight {
        background-color: #f1f1f1;
        border-left: 4px solid #009688;
        padding: 10px 15px;
        font-size: 0.95em;
        color: #444;
        margin: 1em 0;
        border-radius: 4px;
      }
    </style>
  </head>
  <body>
    <div class="email-container">
      <h2>📋 Pure Health Submission</h2>
      <div class="highlight">A new appointment intake form has been submitted successfully.</div>

      <h3>👤 Patient Details</h3>
      <ul>
        <li><strong>Name:</strong> <%= @data["firstName"] %> <%= @data["lastName"] %></li>
        <li><strong>Email:</strong> <%= @data["email"] %></li>
        <li><strong>Phone:</strong> <%= @data["phone"] %></li>
        <li><strong>Gender:</strong> <%= @data["gender"] %></li>
        <li><strong>Date of Birth:</strong> <%= @data["dobMonth"] %>/<%= @data["dobDay"] %>/<%= @data["dobYear"] %></li>
        <li><strong>Driver’s License:</strong> <%= @data["driversLicense"] %></li>
        <li><strong>Occupation:</strong> <%= @data["occupation"] %></li>
        <li><strong>Marital Status:</strong> <%= @data["maritalStatus"] %></li>
        <li><strong>Children:</strong> <%= @data["hasChildren"] %>
          <% if @data["hasChildren"] == "yes" %>(<%= @data["childrenCount"] %> kids)<% end %>
        </li>
      </ul>

      <h3>🏠 Address</h3>
      <ul>
        <li><strong>Street:</strong> <%= @data["streetAddress"] %></li>
        <li><strong>City:</strong> <%= @data["city"] %></li>
        <li><strong>State:</strong> <%= @data["state"] %></li>
        <li><strong>Zip Code:</strong> <%= @data["zipCode"] %></li>
      </ul>

      <h3>🛌 Health & Lifestyle</h3>
      <ul>
        <li><strong>Health Goals:</strong> <%= @data["healthGoals"] %></li>
        <li><strong>Current Medications:</strong> <%= @data["currentMedications"] %></li>
        <li><strong>Allergies:</strong> <%= @data["allergies"] %></li>
        <li><strong>Hours of Sleep:</strong> <%= @data["hoursOfSleep"] %></li>
        <li><strong>Height:</strong> <%= @data["height"] %></li>
        <li><strong>Weight:</strong> <%= @data["weight"] %></li>
        <li><strong>Exercise Days:</strong> <%= @data["exerciseDays"] %></li>
        <li><strong>Last Time Felt Well:</strong> <%= @data["lastFeltWell"] %></li>
        <li><strong>Hormone Therapy Before:</strong> <%= @data["hormoneTherapyBefore"] %></li>
        <li><strong>Motivation:</strong> <%= @data["motivation"] %></li>
      </ul>

      <h3>🧬 Medical History</h3>
      <ul>
        <li><strong>Conditions:</strong> <%= Array(@data["conditions"]).join(", ") %></li>
        <li><strong>Family History:</strong> <%= Array(@data["familyHistory"]).join(", ") %></li>
        <li><strong>Social History:</strong> <%= Array(@data["socialHistory"]).join(", ") %></li>
        <li><strong>Symptoms:</strong> <%= Array(@data["symptoms"]).join(", ") %></li>
      </ul>

      <h3>🛠 Services Requested</h3>
      <ul>
        <li><strong>Services:</strong> <%= Array(@data["services"]).join(", ") %></li>
      </ul>

      <h3>🖊 Consents</h3>
      <ul>
        <li><strong>Medical Consent Date:</strong> <%= @data["patientConsentForMedicalServicesDate"] %></li>
        <li><strong>HGH Consent Date:</strong> <%= @data["informedConsentForHghReplacementTherapyPatientDate"] %></li>
        <li><strong>Testosterone Consent Date:</strong> <%= @data["informedConsentForTestosteroneReplacementTherapyPatientDate"] %></li>
        <li><strong>Controlled Substance Refill DOB:</strong> <%= @data["controlledSubstanceAutoRefillConsentDob"] %></li>
      </ul>

      <div class="footer">
        <p>This is a submission from the Pure Health & Wellness intake portal.</p>
      </div>
    </div>
  </body>
</html>
